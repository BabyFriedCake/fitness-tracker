# Migration Instructions

Copy:

- `AGENTS.md` to the repository root
- `docs/08-Development/` to the repository docs directory

Suggested commit:

```bash
git add AGENTS.md docs/08-Development
git commit -m "docs: define mobile development and Codex workflow"
git push
```
